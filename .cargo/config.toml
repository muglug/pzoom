# getrandom 0.3 (pulled in via rand) requires an explicit backend on
# wasm32-unknown-unknown; pair with the `wasm_js` feature enabled by
# the pzoom-wasm crate.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
